<template xmlns="http://ws.apache.org/ns/synapse" name="helloRequest">
    <parameter name="name" description="Name for grpc request"/>
    <parameter name="responseVariable" description="The name of the variable to which the response should be stored."/>
    <parameter name="overwriteBody" description="Replace the Message Body in Message Context with the response of the operation."/>
    <sequence>
    <!-- Possibly an In mediator or other mediators that read the inbound request -->

    <class name="org.wso2.carbon.payloadvconnector.PersonMediator">
        <property name="name" expression="$func:name"/>
        <!-- <property name="name" value="name,"/> -->
        <!-- Optional parameters if your mediator has set/get methods for properties -->
    </class>

    <log level="full">
        <property name="AfterMediator" value="Check the property that was set"/>
        <property name="receivedName" expression="get-property('receivedName')"/>
    </log>
</sequence>
</template>
